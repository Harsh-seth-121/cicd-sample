# Build outputs and deps — must be (re)installed inside the image
node_modules
dist
dist-ssr

# VCS / editor / OS
.git
.gitignore
.idea
.vscode
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Docs and local artifacts (not needed for `npm run build`)
docs
README.md

# Don't ship the Dockerfile/.dockerignore themselves into the image
Dockerfile
.dockerignore
